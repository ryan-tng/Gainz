/**
 * Base URL for the Gainz backend (the Next.js app that proxies the AI food
 * scanner to Claude). Set this to your deployed URL once the web app is live
 * on Vercel, e.g. "https://gainz.vercel.app".
 *
 * For local testing against `npm run dev` on your PC, use your machine's LAN IP
 * (not localhost — the phone can't reach the PC's localhost), e.g.
 * "http://192.168.68.51:3000".
 */
export const API_BASE_URL = "https://gainz.vercel.app";
